#include <stdio.h>
#include <math.h>

int main()
{
    int i;
    double log2_fact = 0;

    for(i = 1; i <= 25; i++)
    {
        log2_fact += log2(i);
    }

    printf("log2(25!) = %.2f\n", log2_fact);
    printf("Approximate number of keys = 2^%.0f\n", log2_fact);

    return 0;
}
